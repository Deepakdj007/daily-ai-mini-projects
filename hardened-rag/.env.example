# Free Groq API key, no credit card: https://console.groq.com/keys
GROQ_API_KEY=

# Optional overrides. The defaults are what the leaderboard was measured with.
# Both gpt-oss models hold separate free-tier buckets, so running the whole
# ladder again on the small one is free replication rather than a confound.
# CHAT_MODEL=openai/gpt-oss-120b
# GUARD_MODEL=meta-llama/llama-prompt-guard-2-86m
