# Free Groq API key — no credit card. Create one at https://console.groq.com/keys
GROQ_API_KEY=

# Optional. Set to 1 to run with canned model responses and make zero Groq calls.
# The --stub flag on `src.night run` does the same thing for a single run.
NIGHT_SCOUT_STUB=
