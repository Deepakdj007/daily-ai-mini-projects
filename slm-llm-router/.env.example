# Free Groq API key: https://console.groq.com/keys
GROQ_API_KEY=

# Optional. Set SLM_PROVIDER=groq to run without Ollama at all (tier D):
# the small tier becomes openai/gpt-oss-20b, which is only 2x cheaper than the
# large tier, so expect the savings number to fall from ~90% to ~50%.
# SLM_PROVIDER=ollama
# SLM_OLLAMA_MODEL=qwen3.5:4b
# OLLAMA_HOST=http://localhost:11434
